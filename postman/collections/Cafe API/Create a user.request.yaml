$kind: http-request
name: Create a user
url: "{{base_url}}/users"
method: POST
body:
  type: json
  content: |-
    {
        "email": "{{user_email}}",
        "name": "{{user_name}}"
    }
order: 4463455141885054
