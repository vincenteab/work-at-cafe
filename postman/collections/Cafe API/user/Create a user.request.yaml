$kind: http-request
name: Create a user
url: "{{base_url}}/users/register"
method: POST
body:
  type: json
  content: |-
    {
        "email": "{{user_email}}",
        "name": "{{user_name}}",
        "password": "{{user_password}}"
    }
order: 818325309582453
